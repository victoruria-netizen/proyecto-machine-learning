# Dependencias del proyecto — Predicción de Siniestros Viales (PAA)
# Python 3.13.2 (ver README). Versiones exactas por reproducibilidad (guía PAA, entregables 3 y 4).
# Corregido el 2026-09-24: "3.13.15" (documentado desde el 2026-09-20) no es una versión de
# Python que exista — `uv python install 3.13.15` no la encuentra. Era un error de tipeo que
# nadie había verificado desde entonces. El estándar pasa a ser 3.13.2, la que ya tenía instalada
# el .venv del proyecto y con la que corrieron las últimas sesiones. Ver HANDOFF.md.
#
# Instalación (reproduce el entorno completo, incluidas las dependencias transitivas):
#   pip install -r requirements.txt -c requirements-lock.txt
#
# Este archivo lista SOLO lo que los notebooks importan (más lo necesario para ejecutarlos).
# requirements-lock.txt fija el árbol completo (pip freeze del entorno verificado) y se usa
# como "constraints": no agrega paquetes, solo fija sus versiones.
# Si se necesita una biblioteca nueva: agregarla acá con versión exacta, instalar y regenerar el lock.
#
# skforecast se quitó el 2026-09-24 (ver HANDOFF.md): dejó de usarse al retirar base_2.ipynb
# (reemplazado por notebooks/linea_base.ipynb, que arma el árbol candidato a mano, sin
# skforecast). Las restricciones de abajo (pandas <3, matplotlib <3.12, statsmodels <0.15) ya NO
# aplican por ese motivo — quedan igual porque subir versiones obliga a reejecutar y revalidar
# todo, y es una decisión aparte, pendiente para el Entregable 3.

# --- Análisis y manejo de datos ---
numpy==2.5.2
pandas==2.3.3
scipy==1.18.1            # interpolación y pruebas estadísticas

# --- Visualización ---
matplotlib==3.11.1

# --- Aprendizaje automático y series temporales ---
scikit-learn==1.9.0      # baselines, métricas, árbol de decisión
statsmodels==0.14.6      # diagnóstico de series temporales

# --- Variables exógenas ---
holidays==0.103          # feriados de Uruguay para el calendario
requests==2.34.2         # carga histórica puntual de Open-Meteo (cacheada)

# --- Geo / mapas ---
geopandas==1.1.4         # cruce punto-polígono con los municipios (diagnostico_datos_municipio)
pyproj==3.7.2            # conversión de coordenadas UTM 21S (EPSG:32721) a lat/lon
pyshp==3.1.6             # lectura de shapefiles; se importa como `shapefile`
folium==0.20.0           # mapa de zonas del ETL
branca==0.8.2            # escalas y leyendas de los mapas folium

# --- Notebooks ---
jupyter==1.1.1           # incluye nbconvert (jupyter nbconvert --execute)
ipykernel==7.3.0
ipython==9.17.1          # los notebooks importan IPython.display
